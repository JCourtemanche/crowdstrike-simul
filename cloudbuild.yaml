steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/crowdstrike-simul/crowdstrike-simul:latest'
      - '-f'
      - 'deployment/Dockerfile'
      - '.'

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/crowdstrike-simul/crowdstrike-simul:latest'

substitutions:
  _REGION: europe-west1
